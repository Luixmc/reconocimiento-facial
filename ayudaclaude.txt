Veo el problema exacto en el código que pegaste. El bloque try/except ImportError está roto — los métodos __init__, update_config, get_frame y todos los demás quedaron fuera de la clase con indentación incorrecta. Python los trata como funciones sueltas, no como métodos de SourceManager.
Mira esto en el archivo:
pythonclass SourceManager:
    ...
    STREAM_PATHS = []  # se inicializa abajo

# ← AQUÍ termina la clase (sin indentación)
try:
    from network_scanner import STREAM_PATHS as _STREAM_PATHS
    SourceManager.STREAM_PATHS = _STREAM_PATHS
except ImportError:
    SourceManager.STREAM_PATHS = ["/video", "/mjpeg", "/"]

    # ← AQUÍ los métodos tienen indentación pero están DENTRO del except, no de la clase
    def __init__(self) -> None:
Te genero el archivo corregido directamente:Se ejecutaron 2 comandos, archivo leídoSe ejecutaron 2 comandos, archivo leídoSintaxis verificada — 440 líneas, sin errores. Descárgalo y reemplaza el que tienes en backend/.
Lo que se corrigió: el bloque try/except ImportError de network_scanner estaba en medio de la clase, lo que hacía que todos los métodos (__init__, update_config, get_frame, etc.) quedaran fuera de SourceManager. Ahora ese bloque está al final del archivo, después de cerrar la clase correctamente. También agregué stop() como alias de close() para que no vuelva a haber conflicto de nombres.